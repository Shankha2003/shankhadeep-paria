
import React from 'react';

const BackgroundRays: React.FC = () => {
  return (
    <div className="ray-container">
      {/* The background is handled via the CSS class 'ray-container' in index.html for performance */}
    </div>
  );
};

export default BackgroundRays;
